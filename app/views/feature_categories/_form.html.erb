<%= form_for(@feature_category) do |f| %>
  <% if @feature_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feature_category.errors.count, "error") %> prohibited this feature_category from being saved:</h2>

      <ul>
      <% @feature_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
